# algorithms-site — Вахтенный журнал

**Версия:** 1.2.3 · **Дата:** 2026-08-28 · **Вахта:** A

> **Что это.** Точка входа: где стоим и что дальше. Строка «Версия» выше **читается
> машиной** — `deploy.sh` сверяет её с `VERSION` и не публикует репу при расхождении.
> Полный протокол — `_base/00-infrastructure/04-watchlog-protocol.md`.

---

## §0. Где стоим

- **Текущая точка: v1.2.3** (2026-08-28) — служебный блок «Структура» убран
  из README (внутренний артефакт `base-repo`), `.repo-meta` `private`
  приведён к факту (`PUBLIC`).
- v1.2.0 (2026-08-28) — тёмная/светлая тема, переключатель
  в шапке, `localStorage`, токенизированная палитра. Проверено вживую
  (Chrome DevTools MCP): переключение, персистентность после reload,
  визуализация и проверка задачи работают без регрессий в обеих темах.

**Следующий шаг:** P2 — Pyodide вместо самодельного транслятора (закрывает
главный потолок) или P1 — девять новых уроков по образцу.

## §1. Последние изменения

- **2026-08-28** — v1.2.0: тёмная/светлая тема, проверено вживую.
- **2026-08-23** — v1.0.0: журнал заведён; версия и точка входа сведены.

## §2. Находки и грабли

_пока пусто_
